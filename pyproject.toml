[project]
name = "openvino-face-skill"
version = "0.1.0"
description = "Standalone OpenVINO face embedding DB (enroll + identify)."
requires-python = ">=3.8"
dependencies = [
  "numpy>=1.24",
  "opencv-python>=4.8",
  "openvino>=2025.1.0",
]

